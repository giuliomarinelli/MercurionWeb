<style>
  :root { color-scheme: light dark; }
  body {
    margin: 0;
    padding: 24px 0;
    background-color: #f8fafc;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", system-ui, sans-serif;
  }
  .email-wrapper {
    max-width: 560px;
    margin: 0 auto;
    background-color: #F9FAFB;
    border-radius: 12px;
    border: 1px solid #E5E7EB;
    padding: 24px 24px 20px;
  }
  @media (prefers-color-scheme: dark) {
    body { background-color: #0a0a0a; }
    .email-wrapper { background-color: #1F2937; border-color: #4B5563; }
  }
  .logo {
    display: none;
    text-align: left;
    margin-bottom: 24px;
    padding: 12px;
    background-color: #0a0a0a;
    border: 1px solid #696e76;
    border-radius: 15px;
    width: fit-content;
    box-shadow: 1px 1px 5px 1px #919191;
    margin: auto;
  }
  h1 { margin: 0 0 8px; font-size: 22px; line-height: 1.3; color: #11141D; }
  p { margin: 0 0 12px; font-size: 15px; line-height: 1.6; color: #4B5563; }
  .button-wrapper { margin: 24px 0 12px; }
  .btn-primary {
    display: inline-block;
    padding: 10px 20px;
    border-radius: 999px;
    background-color: #2563EB;
    color: #ffffff !important;
    font-weight: 600;
    font-size: 14px;
    text-decoration: none;
    text-align: center;
    min-width: 180px;
  }
  .muted { font-size: 13px; color: #6B7280; }
  .fallback-link { word-break: break-all; font-size: 13px; color: #2563EB; }
  .footer {
    margin-top: 24px;
    border-top: 1px solid #E5E7EB;
    padding-top: 12px;
    font-size: 11px;
    color: #9CA3AF;
  }
  @media (prefers-color-scheme: dark) {
    h1 { color: #F3F4F6; }
    p { color: #D1D5DB; }
    .muted { color: #9CA3AF; }
    .fallback-link { color: #60A5FA; }
    .footer { border-top-color: #4B5563; color: #6B7280; }
  }
</style>
