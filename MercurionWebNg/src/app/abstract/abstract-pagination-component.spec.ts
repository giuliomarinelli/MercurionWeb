import { AbstractPaginationComponent } from './abstract-pagination-component';

describe('AbstractPaginationComponent', () => {
  it('should create an instance', () => {
    expect(new AbstractPaginationComponent()).toBeTruthy();
  });
});
