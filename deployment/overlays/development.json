{
  "environment": "development",
  "services": {
    "web-ng": { "enabled": true, "target": "development", "env": { "NODE_ENV": "development", "APP_ENV": "development" } },
    "web-node": { "enabled": true, "target": "development", "env": { "NODE_ENV": "development", "APP_ENV": "development", "APP_PORT": "8099", "APP_HOST": "0.0.0.0" } },
    "redis": { "enabled": true, "env": { "REDIS_PASSWORD": "${REDIS_PASSWORD}" } },
    "nats": { "enabled": true, "env": {} },
    "postgres": { "enabled": true, "env": { "POSTGRES_USER": "${POSTGRES_USER}", "POSTGRES_PASSWORD": "${POSTGRES_PASSWORD}", "POSTGRES_DB": "${POSTGRES_DB}" } },
    "meilisearch": { "enabled": true, "env": { "MEILI_MASTER_KEY": "${MEILI_MASTER_KEY}" } },
    "nginx": { "enabled": true, "env": {} },
    "tox21": { "enabled": true, "env": { "PY_ENV": "development", "NATS_URL": "nats://localhost:4223", "VERSION": "${TOX21_VERSION}" } }
  }
}
