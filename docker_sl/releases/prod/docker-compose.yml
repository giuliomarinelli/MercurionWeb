services:
  mercurion_web_ng_prod:
    build:
      context: ../../..
      dockerfile: MercurionWebNg/Dockerfile
      target: production
      args:
        BUILD_VERSION: ${BUILD_VERSION}
        BUILD_REVISION: ${BUILD_REVISION}
    image: ${REGISTRY_IMAGE_NG}:${BUILD_VERSION}-${BUILD_REVISION}
    user: "10101:10101"
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=64m
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  mercurion_web_node_prod:
    build:
      context: ../../..
      dockerfile: MercurionWebNode/Dockerfile
      target: production
      args:
        BUILD_VERSION: ${BUILD_VERSION}
        BUILD_REVISION: ${BUILD_REVISION}
    image: ${REGISTRY_IMAGE_NODE}:${BUILD_VERSION}-${BUILD_REVISION}
    user: "10001:10001"
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=64m
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
