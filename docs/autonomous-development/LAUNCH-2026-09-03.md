# Launch record: 24-hour SYS-008 recovery session 2026-09-03

This one-time run is configured by:

```text
docs/autonomous-development/session.24h-2026-09-03.yaml
```

Its soft deadline is `2026-09-04T12:00:00+02:00`. Launch near 12:00 CEST on
2026-09-03 to provide the intended 24-hour window. At the deadline the
coordinator starts no new recipe, completes the active task's safe lifecycle,
writes and pushes the report, waits for its exact CI, and stops.

## Recovery gate

Launch only after recovery pull request #28 is merged and the exact resulting
`origin/develop` SHA has a successful Windows/Linux `Required gate`. Local
`develop` must be clean and equal `origin/develop`.

Task `0008` has direct human authorization for a fresh retry. Its failed
attempt is retained read-only at `archive/SYS-008-attempt-2026-09-03`, exact
SHA `b99d864ef8c8555fe9f28bfa9152cd6581f78720`. No local or remote
`feature/SYS-008` may exist before launch. The archived attempt may be
inspected or selectively reimplemented but must not be cherry-picked wholesale.

The 108 dependency skips caused by the first `SYS-008` attempt have been
deliberately reset to pending. The new lazy dependency policy must leave later
recipes unchanged unless each one reaches its normal selection point while a
hard prerequisite is still terminal non-`DONE`.

Run the canonical root gate before launching:

```text
npm ci
npm run ci:check
git diff --check
git status --short
```

The final status must be clean. Start a fresh GitHub Copilot CLI parent process
from the MercurionWeb repository root:

```text
copilot --agent development-session-coordinator --allow-all-tools --allow-all-urls --add-dir ../MercurionTox21 --reasoning-effort high --autopilot
```

Inside that fresh CLI, verify the parent session before pasting the prompt:

```text
/model
/permissions show
/mcp list
/keep-alive on
```

Require GPT-5.6 Sol with High reasoning, the intended unattended permissions,
the connected `chrome-devtools` MCP server, and keep-alive. Do not start a
second coordinator against the same checkout.

## Starting prompt

```text
Run the complete autonomous Development Session defined by `docs/autonomous-development/session.24h-2026-09-03.yaml` as the `Development Session Coordinator`.

First read the active YAML, `AGENTS.md`, `docs/autonomous-development/PROTOCOL.md`, `docs/autonomous-development/RUNTIME.md`, and `docs/autonomous-development/CI-BASELINE.md` in full. Validate every startup precondition before making repository writes. Refuse to launch if pull request #28 is not represented in `origin/develop`, the exact `develop` SHA does not have a successful Windows/Linux `Required gate`, either autonomous validator fails, the deadline has passed, the tree/base/authentication/capabilities are unsafe, `feature/SYS-008` already exists locally or remotely, or a required decision remains unresolved. Pull requests #25, #27 and #28 are immutable provenance and must not be mutated.

Before any task branch or recipe work, perform the configured real isolated npm capability probe, delete exactly its temporary directory, prove repository status is clean and unchanged, verify repository-local `commit.gpgSign=false`, and use `--no-gpg-sign` for every commit-producing command. Make exactly one non-mutating synchronous `task` handshake with `agent_type: development-task-worker`, `mode: sync`, `capability_probe: true`, and a fresh nonce; require exactly `TASK_CAPABILITY_OK <nonce>`. Verify `task_complete` and `.github/mcp.json` before implementation.

Prove the permanent baseline from the repository root with `npm ci` and `npm run ci:check` before creating the first task branch. Before every clean install, prove all session/task-owned Angular, Nest, Tox21, test watcher and other workspace-consuming processes are stopped. Never start runtime processes speculatively. Runtime is task-scoped: start it only after the unchanged task-start preflight and implementation when declared browser evidence is required, then stop it before the final `npm ci` plus `npm run ci:check` gate. A missing, red, cancelled or unverifiable baseline is a session-level incident: stop without changing a recipe outcome.

Task 0008 is explicitly re-enabled by direct human authorization for this new session. Its previous attempt is archived read-only at `archive/SYS-008-attempt-2026-09-03`, exact SHA `b99d864ef8c8555fe9f28bfa9152cd6581f78720`. Create a fresh `feature/SYS-008` from the proven-green current `develop`. Inspect and selectively reimplement useful parts of the archived attempt, but do not cherry-pick it wholesale or advance/mutate the archive. Reconfirm generated Angular TypeScript is byte-stable on Windows and Linux without weakening `graphql-codegen --check`.

Process pending tasks from the configured Series in filename order, exactly one synchronous worker at a time. Evaluate dependency skips lazily: never precompute or materialize a transitive closure. Only when one pending task reaches its normal selection point with a terminal non-DONE hard prerequisite may that one task be marked SKIPPED_DEPENDENCY; leave every later recipe unchanged, wait for exact metadata CI, and restart selection.

Push each final feature SHA and require its exact Windows/Linux `Required gate` before merge. Only then perform a no-fast-forward, no-GPG-sign merge into `develop`, push, and wait for exact merge-SHA CI. Apply the complete DONE, BLOCKED, REVERTED, lazy dependency-skip, branch-preservation, revert, reporting, and finalization lifecycle from the committed protocol. Never use background workers or run two task workers concurrently.

Operate autonomously within the committed rules and do not request approval for routine in-scope operations. Never touch `master`, deploy, publish, use production credentials/data, force-push, rebase, rewrite history, or mutate pull requests #25, #27 or #28.

At `2026-09-04T12:00:00+02:00`, start no new task. Finish the full safe lifecycle of the active task, generate and push the configured report on clean `develop`, wait for exact report-commit CI, emit the concise final summary and report path, then call `task_complete` as the final action and produce no further prose or tool calls.
```

This dated configuration and prompt must never be reused at or after their
deadline.
