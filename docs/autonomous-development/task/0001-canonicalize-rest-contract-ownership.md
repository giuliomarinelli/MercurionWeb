# 0001 - Canonicalize REST contract ownership

- [x] DONE
- [ ] BLOCKED
- [ ] REVERTED
- [ ] SKIPPED_DEPENDENCY

## Objective

Eliminate manually duplicated Angular REST wire definitions so every public
request/response shape derives from a versioned framework-neutral shared
package, while retaining checked Nest validation adapters where runtime
validation or transformation requires them.

Add the shared contract package to the existing repository-root npm workspace
and preserve its already-green single-lockfile dependency topology and
permanent CI baseline.

Source: `SYS-001` in Series `0001`.

## Context

The audit found REST DTOs duplicated between
`MercurionWebNg/src/app/Models/**` and Nest DTOs under
`MercurionWebNode/src/**/Models/DTO/**`. Angular services using `HttpClient`
include auth, account, feedback, history, country, recovery, Mercurion AI, and
RDKit clients.

Repository-wide baseline construction and CI bootstrap are no longer part of
this recipe. They are the external session prerequisite documented in
`docs/autonomous-development/CI-BASELINE.md`. Task `0001` starts only from a
clean `develop` whose exact SHA is already green on permanent Windows/Linux
GitHub Actions.

## Relevant files and modules

- `MercurionWebNg/src/app/Models/`
- Angular REST client services under `MercurionWebNg/src/app/services/`
- `MercurionWebNode/src/**/Models/DTO/`
- Nest REST controllers, serializers, and boundary mappers
- root and project package manifests plus the root lockfile
- `packages/rest-contracts/`
- deterministic contract/parity checks
- `.github/workflows/ci.yml`, only as required to register contract checks

## In scope

- Inventory public REST request/response payloads consumed by Angular.
- Establish one versioned, framework-neutral shared contract package.
- Replace manual client-side wire-shape copies with shared types.
- Retain Nest-only decorated validation/transformation DTOs as checked boundary
  adapters where required.
- Add deterministic compile-time and/or runtime contract-parity checks.
- Add `packages/rest-contracts` to the existing root workspace.
- Update the existing root lockfile only through the pinned npm toolchain.
- Register contract checks without removing the workflow's Windows/Linux
  matrix, feature-branch trigger, `develop` trigger, or stable aggregate gate.

## Out of scope

- Constructing or repairing the repository-wide CI baseline.
- Absorbing unrelated lint, type, test, build, or dependency debt.
- GraphQL and WebSocket contracts.
- Unrelated API redesigns or endpoint renames.
- Changes to `../MercurionTox21`.
- Disabling, weakening, or bypassing an existing CI gate.

## Decisions already made

- The permanent green baseline is established outside the numbered workload.
- The canonical REST source is a versioned, framework-neutral shared package.
- Angular never imports Nest transport classes or depends on
  `class-validator`, `class-transformer`, or another Nest runtime concern.
- Decorated Nest DTO classes may remain at the HTTP boundary when required.
  They are adapters, not an independently evolving canonical contract.
- Deduplication is incremental and non-breaking.
- Existing externally visible REST behaviour is preserved unless another task
  explicitly changes it.
- The root lockfile is generated by pinned npm from a clean dependency tree and
  is never manually edited.

## Requirements

1. Enumerate Angular `HttpClient` calls and the Nest endpoints they consume.
2. Identify duplicated request/response types.
3. Separate portable wire semantics from Nest validation/transformation
   concerns.
4. Define every public REST wire payload in the shared package without Angular
   or Nest runtime dependencies.
5. Migrate Angular consumers to shared types.
6. Keep Nest as the runtime validator and constrain retained adapters to the
   shared contract.
7. Add deterministic checks that fail when clients, adapters, serializers, or
   mappers drift.
8. Preserve current validation, transformation, serialization, and JSON shape.
9. Add the shared package to the existing root workspace and preserve its one
   reproducible lockfile/install flow.
10. Preserve the complete permanent CI contract throughout the change.

## Acceptance criteria

- [ ] Every public REST payload consumed by Angular has one canonical shared
  wire definition.
- [ ] Angular has no handwritten duplicate of a migrated wire DTO.
- [ ] Angular has no Nest or validation-framework runtime dependency.
- [ ] Necessary Nest decorated DTOs are checked boundary adapters.
- [ ] Existing REST validation, transformation, serialization, and JSON shapes
  remain compatible.
- [ ] Contract-parity checks are deterministic and pass twice without changing
  tracked files.
- [ ] The existing root workspace clean-install flow remains reproducible.
- [ ] CI still runs on Windows and Ubuntu for feature branches and `develop`.
- [ ] The exact final feature SHA passes the permanent remote CI before merge.
- [ ] The complete local and remote CI-parity gates pass without unrelated
  baseline remediation.

## Validation

At task start, run root `npm ci` and `npm run ci:check` as documented in
`docs/autonomous-development/CI-BASELINE.md`. They must already be green; a
failure is not permission to repair unrelated repository debt inside SYS-001.

After adding the shared package, regenerate the root lockfile only through a
clean install using the pinned toolchain. Run the root contract checks twice,
the complete `ci:check` suite, and verify the second run leaves the tree
unchanged. Push the feature SHA and require its Windows/Linux GitHub Actions
workflow to succeed before integration.

Task `0008` later expands the existing root aggregate with GraphQL and other
generated-artifact gates; it must not recreate a missing CI control plane.

## Browser validation

Not applicable. Contract ownership and compatibility are established through
compile-time checks, contract tests, service/controller tests, and CI.

## Stop conditions

Mark `BLOCKED` if a payload cannot be migrated without an unauthorized public
API change, if a Nest runtime validation adapter cannot be kept compatible, if
the updated root workspace cannot reproduce installs on both CI platforms, or
if the exact feature-SHA workflow does not succeed.

An unrelated failure already present on the starting `develop` baseline is a
session-level invariant violation and must not be assigned to this task.

## Dependencies

- None. The permanent exact-SHA green baseline is a session launch invariant,
  not a numbered task dependency.

## Implementation notes

Prefer a non-breaking migration. Share portable transport semantics without
forcing framework-specific classes into the common package. Preserve the
existing root install contract and permanent workflow while registering the
new package and contract gates.

## Execution notes

### Previous attempt

The 2026-09-01 session completed local bootstrap and SYS-001 implementation on
`feature/SYS-001`, preserved at
`b576281dc4acaf84a1186522b419a10518ca9eea`. Its merge
`42e12ce8c18bbdefd9334ea4aa62342c84051eb8` was reverted after GitHub Actions
run `33571236825` exposed a Windows-generated root lockfile that lacked the
installable Linux `@css-inline/css-inline-linux-x64-gnu` entry.

The rollback restored the pre-merge tree, including the absence of the
workflow created inside that task, so recovery CI could not run. The detailed
evidence remains in the dated 2026-09-01 report under
`docs/autonomous-development/reports/`.

### Retry authorization

On 2026-09-02, a new direct human instruction authorized this recipe to return
to pending for a fresh Development Session after the permanent baseline pull
request is merged and its exact `develop` SHA is green.

The first-attempt commit remains available read-only at
`archive/SYS-001-attempt-2026-09-01`, exact SHA
`b576281dc4acaf84a1186522b419a10518ca9eea`. The former remote
`feature/SYS-001` name was released only after that archive ref was verified.
The coordinator must create a new `feature/SYS-001` from the proven-green
current `develop` base.

The archived implementation may be inspected and selectively reimplemented
when it still satisfies this recipe. It must not be cherry-picked wholesale:
the old attempt also carries superseded workspace, lockfile, bootstrap, and CI
assumptions that are now owned by the permanent baseline.

### 2026-09-02 authorized retry

#### Identity and preflight

- Branch: `feature/SYS-001`.
- Supplied and verified base:
  `e884668a4dc0a3e0c5f9dc62a03bfb95265ef4ee`.
- The branch was clean, matched the supplied base, tracked
  `origin/feature/SYS-001`, and repository-local `commit.gpgSign` was `false`
  before task changes.
- Pinned toolchain: Node.js `22.16.0`, npm `10.9.2`.
- Mandatory unchanged-baseline preflight:
  - `npm ci` — passed; 1740 packages installed and 0 vulnerabilities reported.
  - `npm run ci:check` — passed with exit code 0 before implementation.
  - `git status --short` remained empty and HEAD remained the supplied base.

#### REST inventory and decisions

- `git grep -n -E
  'HttpClient|http\.(get|post|put|patch|delete|request)'
  -- MercurionWebNg/src/app/services` found 58 Angular `HttpClient` calls.
- The inventory was matched to Nest controllers for account, authentication,
  recovery, admin maintenance, countries, feedback, history, embedding,
  Mercurion AI/Tox21, and RDKit.
- Primitive text/boolean/void responses remain primitives. Every structured
  request/response, error body, pagination envelope, header payload, and
  endpoint-specific object consumed by those calls now derives from
  `@mercurion/rest-contracts`.
- The archived attempt was used only as a read-only reference. No commit was
  cherry-picked and none of its superseded workspace bootstrap, dependency
  overrides, lockfile, or single-platform CI assumptions were restored.
- The migration remains non-breaking. Two client compile fixes reflect
  existing server behaviour: embedding results accept either molregno numbers
  or neighbour objects, and registration narrows the UI-only empty gender
  choice before constructing the public request.

#### Implementation

- Added private versioned package `@mercurion/rest-contracts@1.0.0` at
  `packages/rest-contracts`, with no Angular, Nest, validation, transformation,
  or other framework runtime dependency.
- Added the package to the existing root workspace and as an exact `1.0.0`
  dependency of Angular and Nest.
- Replaced Angular handwritten REST DTOs with shared type exports and imported
  shared wire types directly in all REST services. Remaining local extensions
  are UI-only wrappers such as signal-backed history/session models, login
  header metadata, and country flag URLs.
- Preserved Nest decorated DTO classes at validation/transformation boundaries
  and constrained them with shared-contract `implements` declarations plus
  bidirectional compile-time parity assertions. Non-decorated response DTOs
  and pagination/error models now alias the canonical package.
- Typed controller/service mappers and serializers against shared contracts,
  including authentication strategy keys, backup codes, country prefixes,
  embedding results, feedback deletion/serialization, maintenance response,
  Tox21, RDKit, and HTTP errors.
- Added deterministic runtime parity tests proving exact country-prefix mapping
  and exclusion of Nest-only feedback identity fields.
- Registered `npm run ci:contracts` in the root `ci:check` aggregate and as an
  additive step in the existing Windows/Ubuntu workflow. The feature/develop
  triggers, matrix, concurrency policy, read-only permissions, and stable
  `Required gate` remain unchanged.
- Implementation commit:
  `411fd785b5b8f9a708aee328431a9f6b4ea63c51`.

#### Lockfile and task-specific validation

- After package-topology edits, removed the root/workspace dependency trees and
  root lockfile, then ran clean root `npm install` with Node.js `22.16.0` and
  npm `10.9.2`. The generated root lockfile contains the shared workspace link
  and the Linux `@css-inline/css-inline-linux-x64-gnu` optional package.
- `npm run ci:contracts` passed repeatedly. The two required final runs each
  passed the shared package typecheck, Nest bidirectional adapter parity
  typecheck, and 2/2 runtime contract tests.
- Tracked-diff hash before and after both required runs was identical:
  `312a22876db13df1a5156adbaf7fa5d4652fe637`.
- `git grep -n -E '@nestjs|class-validator|class-transformer' --
  MercurionWebNg` returned no matches.
- Angular and Nest typechecks passed after the two intentional client
  compatibility narrowings described above.
- Angular lint passed with the baseline warnings and zero errors. Nest lint
  passed with 61 baseline warnings and zero errors.
- `git diff --check` passed.

#### Complete pre-integration CI parity

- An implementation-stage `npm run ci:check` passed with exit code 0.
- Immediately before readiness, root `npm ci` and `npm run ci:check` were run
  again after this provisional `DONE` status and these notes were present.
  Both passed with exit code 0, covering autonomous-control-plane validation,
  REST contract checks, both lints, both typechecks, all Angular tests, all
  Nest unit and E2E tests, and both production builds.
- Browser validation: not applicable for this contract-ownership task.
- Remote exact-feature-SHA CI and integration remain coordinator-owned;
  provisional `DONE` is therefore `CI_PENDING`.
