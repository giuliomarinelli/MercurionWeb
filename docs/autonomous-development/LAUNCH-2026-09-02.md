# Launch record: overnight Development Session 2026-09-02

This one-time run is configured by:

```text
docs/autonomous-development/session.overnight-2026-09-02.yaml
```

Its soft deadline is `2026-09-03T10:00:00+02:00`. At that instant the
coordinator starts no new recipe, completes the active task's safe lifecycle,
writes and pushes the report, waits for its exact CI, and stops.

## Launch gate

Launch only after pull request `#27` is merged and the exact resulting
`origin/develop` SHA has a successful Windows/Linux `Required gate`. The
working tree must be clean and local `develop` must equal `origin/develop`.

Task `0001` has direct human authorization for a fresh retry. The previous
attempt is retained read-only at
`archive/SYS-001-attempt-2026-09-01`, SHA
`b576281dc4acaf84a1186522b419a10518ca9eea`. No local or remote
`feature/SYS-001` may exist before launch. The archived attempt may be inspected
or selectively reimplemented but must not be cherry-picked wholesale.

Run both structural validators and the canonical root gate before launching:

```text
npm ci
npm run ci:check
git diff --check
git status --short
```

The final status must be clean. Use a current GitHub Copilot CLI and start a
fresh parent process from the MercurionWeb repository root:

```text
copilot --agent development-session-coordinator --allow-all-tools --allow-all-urls --add-dir ../MercurionTox21 --reasoning-effort high --autopilot
```

Inside that fresh CLI, verify the parent session before pasting the prompt:

```text
/model
/permissions show
/mcp list
/keep-alive on
```

Require GPT-5.6 Sol with High reasoning, the intended unattended permissions,
the connected `chrome-devtools` MCP server, and keep-alive. Do not start a
second coordinator against the same checkout.

## Starting prompt

```text
Run the complete autonomous Development Session defined by `docs/autonomous-development/session.overnight-2026-09-02.yaml` as the `Development Session Coordinator`.

First read the active YAML, `AGENTS.md`, `docs/autonomous-development/PROTOCOL.md`, `docs/autonomous-development/RUNTIME.md`, and `docs/autonomous-development/CI-BASELINE.md` in full. Validate every startup precondition before making repository writes. Refuse to launch if pull request #27 is not represented in `origin/develop`, the exact `develop` SHA does not have a successful Windows/Linux `Required gate`, either autonomous validator fails, the deadline has passed, the tree/base/authentication/capabilities are unsafe, `feature/SYS-001` already exists locally or remotely, or a required decision remains unresolved. Pull requests #25 and #27 are immutable provenance and must not be mutated.

Before any task branch or recipe work, perform the configured real isolated npm capability probe, delete exactly its temporary directory, prove repository status is clean and unchanged, verify repository-local `commit.gpgSign=false`, and use `--no-gpg-sign` for every commit-producing command. Make exactly one non-mutating synchronous `task` handshake with `agent_type: development-task-worker`, `mode: sync`, `capability_probe: true`, and a fresh nonce; require exactly `TASK_CAPABILITY_OK <nonce>`. Verify `task_complete` and `.github/mcp.json` before implementation.

Prove the permanent baseline from the repository root with `npm ci` and `npm run ci:check` before creating the first task branch. A missing, red, cancelled, or unverifiable local or exact-SHA remote baseline is a session-level incident: stop without changing a recipe outcome. There is no task 0001 Phase 0 or baseline-repair exception.

Task 0001 is explicitly re-enabled by direct human authorization for this new session. Its previous attempt is archived read-only at `archive/SYS-001-attempt-2026-09-01`, exact SHA `b576281dc4acaf84a1186522b419a10518ca9eea`. Create a fresh `feature/SYS-001` from the proven-green current `develop`. The archived diff may be inspected and selectively reimplemented, but must not be cherry-picked wholesale because it contains superseded workspace, lockfile, bootstrap, and CI assumptions.

Then process pending tasks from the configured Series in filename order, exactly one synchronous worker at a time. Push each final feature SHA and require its exact Windows/Linux `Required gate` before merge. Only then perform a no-fast-forward, no-GPG-sign merge into `develop`, push, and wait for the exact merge-SHA CI. Apply the complete DONE, BLOCKED, REVERTED, dependency-skip, branch-preservation, revert, reporting, and finalization lifecycle from the committed protocol. Never use background workers or run two task workers concurrently.

Operate autonomously within the committed rules and do not request approval for routine in-scope operations. Never touch `master`, deploy, publish, use production credentials/data, force-push, rebase, rewrite history, or mutate pull requests #25 or #27.

At `2026-09-03T10:00:00+02:00`, start no new task. Finish the full safe lifecycle of the active task, generate and push the configured report on clean `develop`, wait for exact report-commit CI, emit the concise final summary and report path, then call `task_complete` as the final action and produce no further prose or tool calls.
```

This dated configuration and prompt must never be reused at or after their
deadline.
