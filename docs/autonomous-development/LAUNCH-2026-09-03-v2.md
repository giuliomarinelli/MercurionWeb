# Launch record: 48-hour continuation session 2026-09-03

This one-time run is configured by:

```text
docs/autonomous-development/session.48h-2026-09-03.yaml
```

Its soft deadline is `2026-09-05T17:00:00+02:00`. At the deadline the
coordinator starts no new recipe, completes the active task's safe lifecycle,
writes and pushes the report, waits for its exact CI, and stops.

## Continuation gate

Launch only when local `develop` is clean, equals `origin/develop`, and that
exact SHA has a successful Windows/Linux `Required gate`. The first pending
recipe must be `0013-canonicalize-contract-enums-and-unions.md` (`SYS-013`).
Tasks `0008` and `0012` are `DONE`; `0009` is `BLOCKED`; `0010` and `0011` are
`SKIPPED_DEPENDENCY`. Those terminal outcomes remain immutable in this new
session. The unchanged `retry_authorization` block in the copied session YAML
records the authorization consumed by the earlier `SYS-008` recovery; it is
historical context, not authorization to reopen task `0008`.

At the time this launch record was authored, `origin/feature/SYS-013` pointed
exactly to `origin/develop` and had no unique commit. After all session startup
probes and the exact-green baseline proof, the coordinator is directly
authorized to delete only `feature/SYS-013`, locally or remotely, if a fresh
check proves that it still points exactly to the proven `origin/develop` SHA
and has no tree or commit divergence. It must then recreate and push the branch
through the normal task lifecycle. If either condition is false, stop without
deleting or overwriting the branch and report the conflict.

Run the canonical root gate before launching:

```text
npm ci
npm run ci:check
git diff --check
git status --short
```

The final status must be clean. Start a fresh GitHub Copilot CLI parent process
from the MercurionWeb repository root:

```text
copilot --agent development-session-coordinator --allow-all-tools --allow-all-urls --add-dir ../MercurionTox21 --reasoning-effort high --autopilot
```

Inside that fresh CLI, verify the parent session before pasting the prompt:

```text
/model
/permissions show
/mcp list
/keep-alive on
```

Require GPT-5.6 Sol with High reasoning, the intended unattended permissions,
the connected `chrome-devtools` MCP server, and keep-alive. Do not start a
second coordinator against the same checkout.

## Starting prompt

```text
Run the complete autonomous Development Session defined by `docs/autonomous-development/session.48h-2026-09-03.yaml` as the `Development Session Coordinator`.

First read the active YAML, `AGENTS.md`, `docs/autonomous-development/PROTOCOL.md`, `docs/autonomous-development/RUNTIME.md`, and `docs/autonomous-development/CI-BASELINE.md` in full. Validate every startup precondition before making repository writes. Refuse to launch if the exact `develop` SHA does not have a successful Windows/Linux `Required gate`, either autonomous validator fails, the deadline has passed, the tree/base/authentication/capabilities are unsafe, or a required decision remains unresolved. Pull requests #25, #27 and #28 are immutable provenance and must not be mutated.

Before any task branch or recipe work, perform the configured real isolated npm capability probe, delete exactly its temporary directory, prove repository status is clean and unchanged, verify repository-local `commit.gpgSign=false`, and use `--no-gpg-sign` for every commit-producing command. Make exactly one non-mutating synchronous `task` handshake with `agent_type: development-task-worker`, `mode: sync`, `capability_probe: true`, and a fresh nonce; require exactly `TASK_CAPABILITY_OK <nonce>`. Verify `task_complete` and `.github/mcp.json` before implementation.

Prove the permanent baseline from the repository root with `npm ci` and `npm run ci:check` before creating the first task branch. Before every clean install, prove all session/task-owned Angular, Nest, Tox21, test watcher and other workspace-consuming processes are stopped. Never start runtime processes speculatively. Runtime is task-scoped: start it only after the unchanged task-start preflight and implementation when declared browser evidence is required, then stop it before the final `npm ci` plus `npm run ci:check` gate. A missing, red, cancelled or unverifiable baseline is a session-level incident: stop without changing a recipe outcome.

The first pending recipe is `0013-canonicalize-contract-enums-and-unions.md`, Source `SYS-013`. Do not reopen terminal tasks `0008` through `0012`. At launch-record authoring time, `origin/feature/SYS-013` was an empty task branch pointing exactly to `origin/develop`. After the startup probes and exact-green baseline proof, fetch and re-check both refs. You have direct human authorization to delete only local or remote `feature/SYS-013` if it still points exactly to the proven `origin/develop` SHA and has no unique commit or tree divergence; then recreate and push it normally before invoking the worker. If it has moved or diverged, do not delete, reset, force-push, reuse, or overwrite it: stop and report the conflict.

Process pending tasks from the configured Series in filename order, beginning with task `0013`, exactly one synchronous worker at a time. Evaluate dependency skips lazily: never precompute or materialize a transitive closure. Only when one pending task reaches its normal selection point with a terminal non-DONE hard prerequisite may that one task be marked SKIPPED_DEPENDENCY; leave every later recipe unchanged, wait for exact metadata CI, and restart selection.

Push each final feature SHA and require its exact Windows/Linux `Required gate` before merge. Only then perform a no-fast-forward, no-GPG-sign merge into `develop`, push, and wait for exact merge-SHA CI. Apply the complete DONE, BLOCKED, REVERTED, lazy dependency-skip, branch-preservation, revert, reporting, and finalization lifecycle from the committed protocol. Never use background workers or run two task workers concurrently.

Operate autonomously within the committed rules and do not request approval for routine in-scope operations. Never touch `master`, deploy, publish, use production credentials/data, force-push, rebase, rewrite history, or mutate pull requests #25, #27 or #28.

At `2026-09-05T17:00:00+02:00`, start no new task. Finish the full safe lifecycle of the active task, generate and push the configured report on clean `develop`, wait for exact report-commit CI, emit the concise final summary and report path, then call `task_complete` as the final action and produce no further prose or tool calls.
```

This dated configuration and prompt must never be reused at or after their
deadline.
